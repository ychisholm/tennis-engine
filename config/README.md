# config

Tuning parameters for the prediction pipeline. YAML or JSON files here control signal weights, decay half-lives, archetype thresholds, and Markov model assumptions. Edit these to recalibrate without touching source code.

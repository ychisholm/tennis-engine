# src/signals

Five signal calculators that each produce a raw edge estimate for a given matchup:

1. Surface affinity
2. Head-to-head record
3. Recent form
4. Fatigue / schedule load
5. Archetype matchup advantage

Each calculator returns a normalized score consumed by the combining layer.

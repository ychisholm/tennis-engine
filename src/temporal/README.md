# src/temporal

Recency decay engine. Applies time-based weighting to historical match data so that recent results carry more influence than older ones. Exposes configurable decay functions (exponential, linear, step).
